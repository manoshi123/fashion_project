// product['price'] = (Math.floor(Math.random() * 78 + 30) * 10 + 9).toFixed(2);;
// product['discount'] = Math.floor(Math.random() * 41) + 20;
// product['compare_at_price'] = (Math.round(product['price'] / (1 - product['discount'] / 100))).toFixed(2);;

// updatedProduct.push(product);